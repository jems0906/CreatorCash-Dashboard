services:
  - type: web
    name: creatorcash-dashboard
    env: static
    buildCommand: npm install; npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
